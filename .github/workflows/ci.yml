name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  evals:
    name: eval metrics unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install pytest pandas requests python-dotenv
      - name: Run dependency-free metric tests
        run: python -m pytest evals/test_metrics.py -v
      - name: Run AV look-ahead filter regression test
        run: python -m pytest tests/test_av_lookahead_filter.py -v

